from django.db import models

class Invoice(models.Model):
    # Champs pour les informations de la facture

class Payment(models.Model):
    # Champs pour les informations de paiement

class InsuranceReimbursement(models.Model):
    # Champs pour les informations de remboursement d'assurance
